# Frontend env (client-side, NEXT_PUBLIC_* are baked at build/dev start).
# In dev, copy to .env.local. In Docker, set via docker-compose environment.

# Base URL for the API. Use relative "/api/v1" when served through nginx (same-origin, recommended).
NEXT_PUBLIC_API_URL=/api/v1
